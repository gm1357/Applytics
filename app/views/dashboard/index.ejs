<%- include('../layout/_header'); %>

    <main role="main" class="container">

            <%if (message != '') { %>
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                    <%= message %>
                    <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
            <%}%>

        <h1 class="text-center">Dashboard</h1>

        <% if (!dados.num_usuarios.totais.length) { %>
            <p>A chave SDK para realizar a integração com o app é: <strong><%= appID %></strong>.</p>
            <p>Integre o SDK no seu app e atualize essa página quando tiver usado pelo menos uma vez seu app para ver os dados.</p>
        <% } else { %>
            <hr>
            <div class="row justify-content-around">
                <div class="shadow bg-white border rounded col-md-2 stats-box text-center">
                    <p>Total usuários</p>
                    <p><strong><%= dados.num_usuarios.totais.length.toString().replace(/\B(?=(\d{3})+(?!\d))/g, ",") %></strong></p>
                </div>
                <div class="shadow bg-white border border-primar rounded col-md-2 stats-box text-center">
                    <p>Tempo médio de sessão</p>
                    <p><strong><%= dados.media_sessao.toFixed(2).toString().replace(/\B(?=(\d{3})+(?!\d))/g, ",") %>s</strong></p>
                </div>
                <div class="shadow bg-white border border-primar rounded col-md-2 stats-box text-center">
                    <p>Total monetário ganho</p>
                    <p><strong>R$ <%= dados.total_gasto[0].sum.toFixed(2).toString().replace(/\B(?=(\d{3})+(?!\d))/g, ",") %></strong></p>
                </div>
                <div class="shadow bg-white border border-primar rounded col-md-2 stats-box text-center">
                    <p>Total de travamentos</p>
                    <p><strong><%= dados.num_crashes.toString().replace(/\B(?=(\d{3})+(?!\d))/g, ",") %></strong></p>
                </div>
            </div>
            
            <div class="row justify-content-around">
                <div class="shadow bg-white border rounded col-md-5 stats-box">
                    <p>Usuários ativos nas ultimas 24h <strong><%= dados.num_usuarios.dia.length.toString().replace(/\B(?=(\d{3})+(?!\d))/g, ",") %></strong></p>
                    <div class="progress">
                        <div class="progress-bar" style="width: <%= (dados.num_usuarios.dia.length / dados.num_usuarios.totais.length) * 100 %>%" role="progressbar" aria-valuenow="<%= dados.num_usuarios.dia.length %>" aria-valuemin="0" aria-valuemax="<%= dados.num_usuarios.totais.length %>"></div>
                    </div>
                    <p>Usuários ativos na última semana <strong><%= dados.num_usuarios.semana.length.toString().replace(/\B(?=(\d{3})+(?!\d))/g, ",") %></strong></p>
                    <div class="progress">
                        <div class="progress-bar" style="width: <%= (dados.num_usuarios.semana.length / dados.num_usuarios.totais.length) * 100 %>%" role="progressbar" aria-valuenow="<%= dados.num_usuarios.semana.length %>" aria-valuemin="0" aria-valuemax="<%= dados.num_usuarios.totais.length %>"></div>
                    </div>
                    <p>Usuários ativos no último mês <strong><%= dados.num_usuarios.mes.length.toString().replace(/\B(?=(\d{3})+(?!\d))/g, ",") %></strong></p>
                    <div class="progress">
                        <div class="progress-bar" style="width: <%= (dados.num_usuarios.mes.length / dados.num_usuarios.totais.length) * 100 %>%" role="progressbar" aria-valuenow="<%= dados.num_usuarios.mes.length %>" aria-valuemin="0" aria-valuemax="<%= dados.num_usuarios.totais.length %>"></div>
                    </div>
                    <p>Usuários ativos no último ano <strong><%= dados.num_usuarios.ano.length.toString().replace(/\B(?=(\d{3})+(?!\d))/g, ",") %></strong></p>
                    <div class="progress">
                        <div class="progress-bar" style="width: <%= (dados.num_usuarios.ano.length / dados.num_usuarios.totais.length) * 100 %>%" role="progressbar" aria-valuenow="<%= dados.num_usuarios.ano.length %>" aria-valuemin="0" aria-valuemax="<%= dados.num_usuarios.totais.length %>"></div>
                    </div>
                    <p>Total de usuários masculinos <strong><%= dados.num_usuarios.masculinos.length.toString().replace(/\B(?=(\d{3})+(?!\d))/g, ",") %></strong></p>
                    <div class="progress">
                        <div class="progress-bar" style="width: <%= (dados.num_usuarios.masculinos.length / dados.num_usuarios.totais.length) * 100 %>%" role="progressbar" aria-valuenow="<%= dados.num_usuarios.masculinos.length %>" aria-valuemin="0" aria-valuemax="<%= dados.num_usuarios.totais.length %>"></div>
                    </div>
                    <p>Total de usuários femininos <strong><%= (dados.num_usuarios.totais.length - dados.num_usuarios.masculinos.length).toString().replace(/\B(?=(\d{3})+(?!\d))/g, ",") %></strong></p>
                    <div class="progress">
                        <div class="progress-bar" style="width: <%= ((dados.num_usuarios.totais.length - dados.num_usuarios.masculinos.length) / dados.num_usuarios.totais.length) * 100 %>%" role="progressbar" aria-valuenow="<%= (dados.num_usuarios.totais.length - dados.num_usuarios.masculinos.length) %>" aria-valuemin="0" aria-valuemax="<%= dados.num_usuarios.totais.length %>"></div>
                    </div>
                </div>

                <div class="shadow bg-white border rounded col-md-5 stats-box">
                    <p>Tempo total de uso do app em segundos <strong><%= dados.tempo_total_sessoes[0].sum.toFixed(2).toString().replace(/\B(?=(\d{3})+(?!\d))/g, ",") %>s</strong></p>
                    <p>Tempo médio de uso do app por usuário <strong><%= dados.media_tempo_total.toFixed(2).toString().replace(/\B(?=(\d{3})+(?!\d))/g, ",") %>s</strong></p>
                    <p>Variância do tempo total de uso do app <strong><%= dados.variancia_tempo_total.toFixed(2).toString().replace(/\B(?=(\d{3})+(?!\d))/g, ",") %>s</strong></p>
                    <p>Desvio padrão do tempo total de uso do app <strong><%= dados.desvio_tempo_total.toFixed(2).toString().replace(/\B(?=(\d{3})+(?!\d))/g, ",") %>s</strong></p>
                    <p>Média de gastos por usuários <strong>R$ <%= dados.media_gasto_total.toFixed(2).toString().replace(/\B(?=(\d{3})+(?!\d))/g, ",") %></strong></p>
                    <p>Variância de gastos por usuários <strong>R$ <%= dados.variancia_gasto_total.toFixed(2).toString().replace(/\B(?=(\d{3})+(?!\d))/g, ",") %></strong></p>
                    <p>Desvio padrão de gastos por usuários <strong>R$ <%= dados.desvio_gasto_total.toFixed(2).toString().replace(/\B(?=(\d{3})+(?!\d))/g, ",") %></strong></p>
                </div>
            </div>

            <div class="row justify-content-around">
                <div class="col-md-11 shadow bg-white border rounded stats-box">
                    <div id="chartUsuarios" style="width:100%; height:400px;"></div>
                    <div id="pieResolucoes" style="width:100%; height:400px;"></div>
                    <div id="barTempoSessao" style="width:100%; height:400px;"></div>
                    <div id="histTempoSessao" style="width:100%; height:400px;"></div>
                    <div id="colIdades" style="width:100%; height:400px;"></div>
                    <div id="mapaDist" style="width:100%; height:400px;"></div>
                    <div id="pieViewsAcessos" style="width:100%; height:400px;"></div>
                    <div id="chartCrashes" style="width:100%; height:400px;"></div>
                </div>
            </div>

            <script>
                Highcharts.setOptions({
                    lang: {
                        months: ['Janeiro', 'Fevereiro', 'Março', 'Abril', 'Maio', 'Junho', 'Julho', 'Agosto', 'Setembro', 'Outubro', 'Novembro', 'Dezembro'],
                        shortMonths: ['Jan', 'Fev', 'Mar', 'Abr', 'Mai', 'Jun', 'Jul', 'Ago', 'Set', 'Out', 'Nov', 'Dez'],
                        weekdays: ['Domingo', 'Segunda', 'Terça', 'Quarta', 'Quinta', 'Sexta', 'Sábado'],
                        loading: ['Atualizando o gráfico...aguarde'],
                        contextButtonTitle: 'Exportar gráfico',
                        decimalPoint: ',',
                        thousandsSep: '.',
                        downloadJPEG: 'Baixar imagem JPEG',
                        downloadPDF: 'Baixar arquivo PDF',
                        downloadPNG: 'Baixar imagem PNG',
                        downloadSVG: 'Baixar vetor SVG',
                        printChart: 'Imprimir gráfico',
                        rangeSelectorFrom: 'De',
                        rangeSelectorTo: 'Para',
                        rangeSelectorZoom: 'Zoom',
                        resetZoom: 'Limpar Zoom',
                        resetZoomTitle: 'Voltar Zoom para nível 1:1',
                    }
                });

                var myLineChart = Highcharts.chart('chartUsuarios', {
                    chart: {
                        type: 'line'
                    },
                    title: {
                        text: 'Fluxo de usuários por mês'
                    },
                    yAxis: {
                        title: {
                            text: 'Número de usuarios'
                        }
                    },
                    xAxis: {
                        categories: ['Janeiro', 'Fevereiro', 'Março', 'Abril', 'Maio', 'Junho', 'Julho', 'Agosto', 'Setembro', 'Outubro', 'Novembro', 'Dezembro']
                    },
                    legend: {
                        align: 'center',
                        verticalAlign: 'top'
                    },
                    series: [{
                        name: 'Usuários novos',
                        data: <%- JSON.stringify(dados.plot.novos) %>
                    }, {
                        name: 'Usuários desistentes',
                        data: <%- JSON.stringify(dados.plot.velhos) %>
                    }],
                    credits: {
                        enabled: false
                    }
                });
                
                var myPieChart = Highcharts.chart('pieResolucoes', {
                    chart: {
                        type: 'pie'
                    },
                    title: {
                        text: 'Distribuição de usuários por resolução de tela'
                    },
                    plotOptions: {
                        pie: {
                            allowPointSelect: true,
                            cursor: 'pointer',
                            dataLabels: {
                                enabled: false
                            },
                            showInLegend: true
                        }
                    },
                    legend: {
                        align: 'center',
                        verticalAlign: 'top'
                    },
                    series: [{
                        name: 'Usuários',
                        colorByPoint: true,
                        data: <%- JSON.stringify(dados.resolucoes) %>
                    }],
                    credits: {
                        enabled: false
                    }
                });

                var myBarChart = Highcharts.chart('barTempoSessao', {
                    chart: {
                        type: 'column'
                    },
                    title: {
                        text: 'Tempo médio de sessão dos usuários'
                    },
                    yAxis: {
                        title: {
                            text: 'Tempo em segundos'
                        }
                    },
                    xAxis: {
                        text: 'Usuários'
                    },
                    legend: {
                        align: 'center',
                        verticalAlign: 'top'
                    },
                    series: [{
                        name: 'Usuários',
                        data: <%- JSON.stringify(dados.bar.tempo_medio_sessao) %>
                    }],
                    credits: {
                        enabled: false
                    }
                });

                var myHistChart = Highcharts.chart('histTempoSessao', {
                    title: {
                        text: 'Histograma do tempo médio de sessão dos usuários'
                    },
                    yAxis: [{
                        title: { text: 'Tempo em segundos' }
                    }, {
                        title: { text: 'Número de usuários'},
                        opposite: true
                    }],
                    xAxis: [{
                        title: { text: 'Número de usuários individuais' }
                    }, {
                        title: { text: 'Tempo em segundos do histograma'},
                        opposite: true
                    }],
                    legend: {
                        align: 'center',
                        verticalAlign: 'top'
                    },
                    series: [{
                        name: 'Histograma',
                        type: 'histogram',
                        xAxis: 1,
                        yAxis: 1,
                        baseSeries: 's1',
                        zIndex: -1
                    }, {
                        name: 'Usuários individuais',
                        type: 'scatter',
                        data: <%- JSON.stringify(dados.bar.tempo_medio_sessao) %>,
                        id: 's1',
                        marker: {
                            radius: 1.1
                        }
                    }],
                    credits: {
                        enabled: false
                    }
                });

                var myColChart = Highcharts.chart('colIdades', {
                    chart: {
                        type: 'column'
                    },
                    title: {
                        text: 'Idade dos usuários'
                    },
                    yAxis: {
                        title: {
                            text: 'Quantidade de usuários'
                        }
                    },
                    xAxis: {
                        categories: <%- JSON.stringify(dados.idades.idadesArray) %>,
                        crosshair: true
                    },
                    legend: {
                        align: 'center',
                        verticalAlign: 'top'
                    },
                    series: [{
                        name: 'Usuários',
                        data: <%- JSON.stringify(dados.idades.quantidade) %>
                    }],
                    credits: {
                        enabled: false
                    }
                });

                var myMap = Highcharts.mapChart('mapaDist', {
                    chart: {
                        map: <%- JSON.stringify(dados.mapGeo) %>
                    },
                    title: {
                        text: 'Distribuição de usuários no mundo'
                    },
                    mapNavigation: {
                        enabled: true,
                        buttonOptions: {
                            verticalAlign: 'bottom'
                        }
                    },
                    colorAxis: {
                        tickPixelInterval: 100
                    },
                    series: [{
                        data: <%- JSON.stringify(dados.paises) %>,
                        keys: ['pais', 'value'],
                        joinBy:['hc-key', 'pais'],
                        name: 'Usuários',
                        states: {
                            hover: {
                                color: '#a4edba'
                            }
                        },
                        dataLabels: {
                            enabled: true,
                            format: '{point.properties.postal}'
                        }
                    }],
                    credits: {
                        enabled: false
                    }
                });

                var myPieViewChart = Highcharts.chart('pieViewsAcessos', {
                    chart: {
                        type: 'pie'
                    },
                    title: {
                        text: 'Distribuição de acessos de telas'
                    },
                    plotOptions: {
                        pie: {
                            allowPointSelect: true,
                            cursor: 'pointer',
                            dataLabels: {
                                enabled: false
                            },
                            showInLegend: true
                        }
                    },
                    legend: {
                        align: 'center',
                        verticalAlign: 'top'
                    },
                    series: [{
                        name: 'Usuários',
                        colorByPoint: true,
                        data: <%- JSON.stringify(dados.views) %>
                    }],
                    credits: {
                        enabled: false
                    }
                });

                var myCrashLineChart = Highcharts.chart('chartCrashes', {
                    chart: {
                        type: 'column'
                    },
                    title: {
                        text: 'Quantidade de travamentos por mês'
                    },
                    yAxis: {
                        title: {
                            text: 'Número de travementos'
                        },
                        stackLabels: {
                            enabled: true,
                            style: {
                                fontWeight: 'bold',
                                color: (Highcharts.theme && Highcharts.theme.textColor) || 'gray'
                            }
                        }
                    },
                    xAxis: {
                        categories: ['Janeiro', 'Fevereiro', 'Março', 'Abril', 'Maio', 'Junho', 'Julho', 'Agosto', 'Setembro', 'Outubro', 'Novembro', 'Dezembro']
                    },
                    legend: {
                        align: 'center',
                        verticalAlign: 'top'
                    },
                    tooltip: {
                        headerFormat: '<b>{point.x}</b><br/>',
                        pointFormat: '{series.name}: {point.y}<br/>Total: {point.stackTotal}'
                    },
                    plotOptions: {
                        column: {
                            stacking: 'normal',
                            dataLabels: {
                                enabled: false,
                                color: (Highcharts.theme && Highcharts.theme.dataLabelsColor) || 'white'
                            }
                        }
                    },
                    series: [{
                        name: 'Travamentos tratados',
                        data: <%- JSON.stringify(dados.crashes_mes_tratadas) %>
                    }, {
                        name: 'Travamentos fatais',
                        data: <%- JSON.stringify(dados.crashes_mes_nao_tratadas) %>
                    }],
                    credits: {
                        enabled: false
                    }
                });
            </script>
        <% } %>
    </main>

<%- include('../layout/_footer'); %>